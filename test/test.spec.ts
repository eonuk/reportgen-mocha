'use strict';

import { expect } from 'chai';

describe('TEST FOLDER Example class', () => {
	it('should create an instance using its constructor', () => {
		expect(1, 'example should exist').to.equal(1); // tslint:disable-line:no-unused-expression
	});
});
